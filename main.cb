from native include raylib;
include game;

class Main
{
    function Main()
    {
        InitWindow(1280, 720, b"Space Invaders");
        SetTargetFPS(60);
        print("\n"*150); //Clear the screen

        game = Game();
    }
}